#!/bin/bash --login
#SBATCH -n 1
#SBATCH -c 1
#SBATCH --mem=8GB
#SBATCH --time=08:00:00
#SBATCH --array=0-8
#SBATCH --output=../array_%A-%a.out

cd ${SLURM_SUBMIT_DIR}

python -u concat_baselines.py -i {SLURM_ARRAY_TASK_ID}